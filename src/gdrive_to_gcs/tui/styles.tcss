#log-panel {
    height: 1fr;
    border: solid $primary;
    padding: 0 1;
}

TransferProgressPanel {
    height: 3;
    dock: bottom;
    padding: 0 1;
    display: none;
}

TransferProgressPanel.active {
    display: block;
}

#transfer-status {
    width: 1fr;
    padding: 0 1;
}
